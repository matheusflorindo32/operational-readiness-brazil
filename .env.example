# This project currently requires no secrets for the protocol itself.
# If future scripts use APIs, store local credentials in .env only.
# NEVER commit .env or real API keys.

# CROSSREF_MAILTO=researcher@example.org
# ZOTERO_LIBRARY_ID=
# ZOTERO_API_KEY=
# OSF_TOKEN=
